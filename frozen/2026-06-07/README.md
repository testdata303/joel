# FROZEN SNAPSHOT — 2026-06-07

This folder is a **complete, self-contained copy** of the JOEL UI as of June 7, 2026,
frozen before any new SMS version is built. **Do not edit anything in this folder.**

## What's here
- `Extension Detail.html` — entry point (open this to view the frozen build)
- `app/` — all React/Babel source as of the freeze
- `assets/` — all CSS as of the freeze
- `CLAUDE.md` — project requirements at freeze time
- `notes/sms-grok-feedback.md` — Grok's feedback on the SMS/contacts build

Relative paths resolve within this folder, so it runs independently of the live project.

## Why
Captures the current **Texting (SMS)** experience and **Contact cards** intact so the new
SMS version can be built fresh in the live project without losing this reference.

New work continues in the project root (`/Extension Detail.html`), NOT here.
